// Creamos nuestras constantes para utilizarlas en los estilos
export const fonts = {
    base: 'system-ui, monospace'
}

export const colors = {
    primary: '#0099ff',
    secondary: '#1c5480',
    black: '#000'
}

// Creamos los breakpoints para los estilos (mobile only)
export const breakpoints = {
    mobile: '520px'
}